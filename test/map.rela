
stuff = { a = 1, b = 2 }

assert(stuff.a == 1)
assert(stuff.b != nil)

stuff.b = nil

assert(stuff.b == nil)
assert(#stuff == 1)

a = {
	af = "alpha",
}

b = {
	bf = "beta",
}

for k,v in b
	a[k] = v
end

assert(a.af == "alpha")
assert(a.bf == "beta")
